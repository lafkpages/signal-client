{
  "name": "@luisafk/signal-client",
  "version": "0.0.1",
  "module": "src/client.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch-config": "bun run scripts/fetch-config.ts",
    "fetch-protos": "bun run scripts/fetch-protos.ts",
    "postinstall": "bun run fetch-config && bun run fetch-protos",
    "format": "prettier --write .",
    "demo": "bun run src/demo"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "@types/bun": "latest",
    "@types/qrcode-terminal": "^0.12.2",
    "prettier": "^3.8.3",
    "protobufjs-cli": "^2.0.1",
    "qrcode-terminal": "^0.12.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@signalapp/libsignal-client": "^0.92.2",
    "protobufjs": "^8.0.1"
  },
  "trustedDependencies": [
    "@signalapp/libsignal-client"
  ]
}
